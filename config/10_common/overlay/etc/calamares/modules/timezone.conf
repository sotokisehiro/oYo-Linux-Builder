[timezone]
# 自動検出を有効化
autoDetect = true

# (オプション) GeoIP のプロバイダーを指定
# 例: "ipinfo" や "ipapi" など。curl があればデフォルトで動きます。
geoipProvider = ipinfo

# (オプション) タイムゾーンが取れないときのフォールバック
defaultTimeZone = Asia/Tokyo
